module.exports = {
    testMatch: [
      '**/tests/**/*.test.js',  // Look specifically for test files inside the tests folder
      '**/*.test.js'  // Also match any .test.js file in the root or subdirectories
    ],
  };
  